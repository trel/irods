
####### Expanded from @PACKAGE_INIT@ by configure_package_config_file() #######
####### Any changes to this file will be overwritten by the next CMake run ####
####### The input file was IRODSConfig.cmake.not_yet_installed.in                            ########

get_filename_component(PACKAGE_PREFIX_DIR "${CMAKE_CURRENT_LIST_DIR}/../../../../" ABSOLUTE)

macro(set_and_check _var _file)
  set(${_var} "${_file}")
  if(NOT EXISTS "${_file}")
    message(FATAL_ERROR "File or directory ${_file} referenced by variable ${_var} does not exist !")
  endif()
endmacro()

macro(check_required_components _NAME)
  foreach(comp ${${_NAME}_FIND_COMPONENTS})
    if(NOT ${_NAME}_${comp}_FOUND)
      if(${_NAME}_FIND_REQUIRED_${comp})
        set(${_NAME}_FOUND FALSE)
      endif()
    endif()
  endforeach()
endmacro()

####################################################################################

list(INSERT CMAKE_MODULE_PATH 0 "${CMAKE_CURRENT_LIST_DIR}/Modules")

set_and_check(IRODS_TARGETS_PATH "${CMAKE_CURRENT_LIST_DIR}/IRODSTargetsWrapper.cmake")
set_and_check(IRODS_TARGETS_PATH_UNWRAPPED "${CMAKE_CURRENT_LIST_DIR}/IRODSTargets.cmake")

if(NOT DEFINED IRODS_BUILD_WITH_CLANG)
  set(IRODS_BUILD_WITH_CLANG "ON")
endif()

if(NOT DEFINED IRODS_PACKAGE_PREFIX)
	set(IRODS_PACKAGE_PREFIX "${PACKAGE_PREFIX_DIR}")
endif()

if(NOT "/opt/irods-externals/clang16.0.6-0" STREQUAL "" AND NOT DEFINED IRODS_EXTERNALS_FULLPATH_CLANG)
  set(IRODS_EXTERNALS_FULLPATH_CLANG "/opt/irods-externals/clang16.0.6-0")
endif()
if(NOT DEFINED IRODS_EXTERNALS_FULLPATH_BOOST)
  set(IRODS_EXTERNALS_FULLPATH_BOOST "/opt/irods-externals/boost1.81.0-2")
endif()
if(NOT DEFINED IRODS_EXTERNALS_FULLPATH_JSONCONS)
  set(IRODS_EXTERNALS_FULLPATH_JSONCONS "/opt/irods-externals/jsoncons0.178.0-0")
endif()
if(NOT DEFINED IRODS_EXTERNALS_FULLPATH_NANODBC)
  set(IRODS_EXTERNALS_FULLPATH_NANODBC "/opt/irods-externals/nanodbc2.13.0-3")
endif()

if(NOT DEFINED IRODS_INSTALL_PREFIX)
	set(IRODS_INSTALL_PREFIX "/")
endif()
if(NOT DEFINED IRODS_PACKAGING_INSTALL_PREFIX)
	set(IRODS_PACKAGING_INSTALL_PREFIX "/")
endif()
if(NOT DEFINED IRODS_INSTALL_BINDIR)
	SET(IRODS_INSTALL_BINDIR "usr/bin")
endif()
if(NOT DEFINED IRODS_INSTALL_SBINDIR)
	SET(IRODS_INSTALL_SBINDIR "usr/sbin")
endif()
if(NOT DEFINED IRODS_INSTALL_LIBEXECDIR)
	SET(IRODS_INSTALL_LIBEXECDIR "usr/libexec")
endif()
if(NOT DEFINED IRODS_INSTALL_SYSCONFDIR)
	SET(IRODS_INSTALL_SYSCONFDIR "etc")
endif()
if(NOT DEFINED IRODS_INSTALL_SHAREDSTATEDIR)
	SET(IRODS_INSTALL_SHAREDSTATEDIR "usr/com")
endif()
if(NOT DEFINED IRODS_INSTALL_LOCALSTATEDIR)
	SET(IRODS_INSTALL_LOCALSTATEDIR "var")
endif()
if(NOT DEFINED IRODS_INSTALL_RUNSTATEDIR)
	SET(IRODS_INSTALL_RUNSTATEDIR "var/run")
endif()
if(NOT DEFINED IRODS_INSTALL_LIBDIR)
	SET(IRODS_INSTALL_LIBDIR "usr/lib")
endif()
if(NOT DEFINED IRODS_INSTALL_INCLUDEDIR)
	SET(IRODS_INSTALL_INCLUDEDIR "usr/include")
endif()
if(NOT DEFINED IRODS_INSTALL_DATAROOTDIR)
	SET(IRODS_INSTALL_DATAROOTDIR "usr/share")
endif()
if(NOT DEFINED IRODS_INSTALL_DATADIR)
	SET(IRODS_INSTALL_DATADIR "usr/share")
endif()
if(NOT DEFINED IRODS_INSTALL_INFODIR)
	SET(IRODS_INSTALL_INFODIR "usr/share/info")
endif()
if(NOT DEFINED IRODS_INSTALL_LOCALEDIR)
	SET(IRODS_INSTALL_LOCALEDIR "usr/share/locale")
endif()
if(NOT DEFINED IRODS_INSTALL_MANDIR)
	SET(IRODS_INSTALL_MANDIR "usr/share/man")
endif()
if(NOT DEFINED IRODS_INSTALL_DOCDIR)
	SET(IRODS_INSTALL_DOCDIR "usr/share/doc/irods")
endif()

if(NOT DEFINED IRODS_HOME_DIRECTORY)
	set(IRODS_HOME_DIRECTORY "var/lib/irods")
endif()
if(NOT DEFINED IRODS_PLUGINS_DIRECTORY)
	set(IRODS_PLUGINS_DIRECTORY "usr/lib/irods/plugins")
endif()
if(NOT DEFINED IRODS_CLIENTS_DIRECTORY)
	set(IRODS_CLIENTS_DIRECTORY "usr/lib/irods/clients")
endif()
if(NOT DEFINED IRODS_DOC_DIR)
	set(IRODS_DOC_DIR "usr/share/doc/irods")
endif()
if(NOT DEFINED IRODS_INCLUDE_DIRS)
	set_and_check(IRODS_INCLUDE_DIRS "${PACKAGE_PREFIX_DIR}/usr/include")
endif()

if(NOT DEFINED IRODS_LINUX_DISTRIBUTION_NAME)
	set(IRODS_LINUX_DISTRIBUTION_NAME "ubuntu")
endif()
if(NOT DEFINED IRODS_LINUX_DISTRIBUTION_VERSION)
	set(IRODS_LINUX_DISTRIBUTION_VERSION "24.04")
endif()
if(NOT DEFINED IRODS_LINUX_DISTRIBUTION_VERSION_MAJOR)
	set(IRODS_LINUX_DISTRIBUTION_VERSION_MAJOR "24")
endif()
if(NOT "noble" STREQUAL "" AND NOT DEFINED IRODS_LINUX_DISTRIBUTION_VERSION_CODENAME)
	set(IRODS_LINUX_DISTRIBUTION_VERSION_CODENAME "noble")
endif()

set(IRODS_COMPILE_DEFINITIONS "linux_platform;_LARGEFILE_SOURCE;_FILE_OFFSET_BITS=64;_LARGE_FILES;_LARGEFILE64_SOURCE")
set(IRODS_COMPILE_DEFINITIONS_PRIVATE "BOOST_SYSTEM_NO_DEPRECATED;BOOST_CHRONO_DONT_PROVIDES_DEPRECATED_IO_SINCE_V2_0_0;BOOST_STACKTRACE_USE_ADDR2LINE;BOOST_ASIO_NO_DEPRECATED;OPENSSL_NO_DEPRECATED_3_0;SPDLOG_FMT_EXTERNAL;SPDLOG_NO_TLS")
set(IRODS_CPACK_DEBIAN_PACKAGE_VERSION "5.0.90")
set(IRODS_CPACK_GENERATOR "DEB")
set(IRODS_CXX_STANDARD "20")
set(IRODS_PACKAGE_DEPENDENCIES_LIST "irods-externals-boost1.81.0-2;irods-externals-nanodbc2.13.0-3")
set(IRODS_PACKAGE_DEPENDENCIES_STRING "irods-externals-boost1.81.0-2, irods-externals-nanodbc2.13.0-3")
set(IRODS_VERSION "5.0.90")
set(IRODS_VERSION_MAJOR "5")
set(IRODS_VERSION_MINOR "0")
set(IRODS_VERSION_PATCH "90")

if(CMAKE_INSTALL_PREFIX_INITIALIZED_TO_DEFAULT OR NOT DEFINED CMAKE_INSTALL_PREFIX)
	set(CMAKE_INSTALL_PREFIX "${IRODS_INSTALL_PREFIX}" CACHE PATH "Install path prefix, prepended onto install directories.")
endif()
if(NOT DEFINED CPACK_PACKAGING_INSTALL_PREFIX)
	set(CPACK_PACKAGING_INSTALL_PREFIX "${IRODS_PACKAGING_INSTALL_PREFIX}" CACHE PATH "Install path prefix for packaging, prepended onto install directories.")
endif()
if(NOT DEFINED CMAKE_INSTALL_LIBDIR)
	SET(CMAKE_INSTALL_LIBDIR "${IRODS_INSTALL_LIBDIR}" CACHE PATH "Object code libraries (lib)")
endif()
